# CODEOWNERS — single-owner stewardship.
#
# Every path is reviewed by @abhinavg6. This is what gates "Require
# review from Code Owners" on the main branch ruleset. Combined with
# "Required approvals = 1" and an empty bypass list, no PR can merge
# without an explicit approval from this account.
#
# When the project grows reviewers, replace the catch-all with
# path-scoped owners (e.g. `/crates/yutha-crypto/ @abhinavg6 @someone`)
# before relaxing the single-approval rule.

*       @abhinavg6
