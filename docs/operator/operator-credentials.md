# Operator credentials

!!! info "Page in progress"
    Full content is being written. Until then the [operator quickstart](quickstart.md) demonstrates the credential flow end-to-end.

Topics this page will cover:

- What an operator credential is and what it can do
- Generating, storing, and rotating the operator key
- Issuing passports to agents
- Revoking an agent (active-stream tear-down semantics)
- Capability cascade on revocation
- Multi-operator setups (deferred; tracked as a follow-on per RFC 0009)
